name: qag-agent-demo
channels:
  - conda-forge
dependencies:
  - python=3.12.12
  - pip=25.3
  - pip:
      - -r requirements-dev.txt
      - -r requirements-mcp.txt
      - -r requirements-cohort-copilot.txt
      - -r requirements-agent.txt
